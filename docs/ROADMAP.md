# Що робимо далі

`REGISTER.md` — хронологічний журнал: що сталося, що виміряно, що виправлено. Він уже
на 123 записах, і планувати за ним неможливо — журнал відповідає на питання «що було»,
а не «що робити». Цей файл — єдиний упорядкований список роботи. Записи журналу
згадуються за номером (#120, #123), не переказуються.

---

## 0. Головна думка, з якої випливає решта

**Більшість наших ознак — це наслідки того, що ми хочемо передбачити.**

RSI, MACD, ковзні середні, смуги — перетворення ціни. Вони не можуть випереджати ціну,
бо вони *і є* ціна, показана під іншим кутом. Макроряди — GDP, CPI, безробіття —
публікуються із затримкою і самі є результатом economічного процесу, а не його
провісником. Ми зібрали 430 ознак, що описують **уже наявний** стан, і виміряли
(#скорочено: звіт `leading_feature_report.py`), що вони не тримають інформації про
майбутнє: найкраща цінова ознака `CURRENT_DRAWDOWN` дала −0.046 в об'єднаному вимірі
й +0.0025 у чесному точці-в-часі всередині тікера — тобто її «сила» була ефектом назви,
а не змісту.

**Випереджає принципово інший клас величин.** Не стан, а:

| клас | приклад | чому випереджає |
|---|---|---|
| позиціонування | CFTC, короткі позиції, опціонний перекіс, ф'ючерсний базис | показує, хто вже мусить діяти далі |
| намір до дії | пошукові запити, перегляди Wikipedia, вакансії, завантаження застосунків | дія настає після наміру |
| **вимушені** майбутні події | ребалансування індексів, закінчення локапів, стіни рефінансування, дати звітів | час відомий наперед, реакція — ні |
| перегляд очікувань | зміни оцінок аналітиків, перегляди звітності, інсайдерські угоди | оновлення переконань випереджає ціну |

**І ключове: ми це вже збираємо.** `cftc`, `wikipedia_attention`, `insider_trades`,
`fear_greed`, `sec_fundamentals` уже в базі. Реєстр фіксує, що вісім увімкнених
збирачів не давали жодної колонки ознак через відсутню передачу даних — виправлено.
Тобто найкращі кандидати, ймовірно, **вже зібрані й жодного разу не виміряні**.

> **Правило, яке з цього випливає:** перш ніж додавати нове джерело — виміряти те,
> що вже збирається. Додавання даних дешеве на вигляд і дороге насправді; вимір
> наявного коштує один прогін звіту.

---

## 1. Порядок робіт

Порядок не за зручністю, а за залежністю: кожен пункт робить наступний вимірюваним.

### 1.1 Дорахувати конвеєр до кінця

- [ ] **Складач має повертати три кадри й ніколи не з'єднувати їх** (#123). Зведений
      кадр — 11.1 ГіБ на 110 іменах, 80% з них NaN, і він чистий проміжок: одразу
      розрізається назад на три файли, які й читає все подальше. Це блокує ширину.
- [ ] Провести стадії 4–7 на 110 іменах. Вони **жодного разу не виконувались** на
      такій ширині; кроки після зведення теж ніколи там не виконувались.
- [ ] Профайлер пам'яті знімає показник **лише на межах фаз** (#123) — усередині
      довгої фази піку не видно. Це вводило в оману чотири рази поспіль. Додати
      замір усередині фаз, інакше наступна діагностика знову буде сліпою.
- [ ] `volatility features` — 82 з 97 секунд на тікер після виправлення адаптивних
      індикаторів (#121). Той самий метод: заміряти, переписати, довести рівність.

### 1.2 Порахувати випереджальність — інструмент уже написаний

`scripts/diagnostics/leading_feature_report.py` рахує точку-в-часі всередині тікера.
Бракувало не інструмента, а **ширини**: на 22 іменах усе давало t = 0.29, що не
відрізняється від шуму.

- [ ] Прогнати звіт на 110 іменах — для **всіх** ознак, не лише цінових.
- [ ] Окремо виміряти те, що ніколи не мірялось: cftc, wikipedia, insider, fear_greed,
      fundamentals. Гіпотеза: саме тут випередження, якщо воно взагалі є.
- [x] ~~**Зробити випереджальність критерієм допуску**, а не post-hoc звітом.~~
      **ПЕРЕПИСАНО 04.09 після Р29: як записано — нереалізовне.**
      `src/config/features.yaml` вмикає **22 збагачувачі, а не ознаки**;
      кожен видає десятки колонок, і важеля «ця ознака входить, бо виміряна»
      в системі немає. Інвентар (`what_has_never_been_measured.py`) показав,
      що гейтити: з 1390 колонок **224 здатні нести крос-секційний сигнал**,
      926 — внутрішньоденні в печатці, 63 константи, 15 із небезпекою
      звʼязків форми Р28. Замість одного пункту — троє воріт:

- [x] ~~**А. Рівень збагачувача** (`features.yaml`).~~ **ЗАКРИТО ВИМІРОМ
      04.09: вимикати нема чого.** Зіставлення зроблено з наявного
      `diagnostic_reports/feature_lineage_report.json` (`component` ->
      `added_columns`), а не новим інструментом. **Одинадцять збагачувачів
      справді мають НУЛЬ придатних колонок — і жоден із них не є мертвим:**

      * **70 колонок ринкові за задумом** (macro 45, market_wide 13, time 12).
        Нуль придатних тут ПРАВИЛЬНО: вони й не мали бути крос-секційними,
        вони входять контекстом і взаємодіями.
      * **24 колонки новин і настроїв — константи 0.0 на всіх 27 доступних
        роках і живі ПІСЛЯ печатки** (`sentiment_ema_1d` дає там 860 різних
        значень, `news_impact_score_1d` 115). Новин до 2024 у нас немає;
        прапорець `*_available = 0` каже саме це. Вимкнути їх означало б
        викинути дані, які збираються на майбутнє.
      * **`EconomicCalendarEnricher` видає нуль колонок** — уже записано як
        #60, не нова знахідка.

      **І фільтрувати їх перед моделюванням теж не треба:**
      `base_trainer._select_features_for_model` ранжує за |кореляцією|, а
      константа дає NaN -> `fillna(-1.0)` -> сортується останньою. Механізм
      уже є, з коментарем, який це пояснює.
- [ ] **Б. Рівень батчу, перед моделюванням.** Колонка з `mode_share >= 0.90`
      не потрапляє в крос-секційну книгу: її ранги — суцільні звʼязки, і
      `sign(rank − 0.5)` відправляє всі імена в один бік. **Це ловить шкоду
      Р28**, де сім таких дали Шарпу 1.016, бувши ринком. Найдешевше й
      найцінніше з трьох.
- [x] ~~**В. Храповик у `tests/contracts/`.**~~ **ЗРОБЛЕНО 04.09** —
      `tests/contracts/test_batch_admission_does_not_rot.py`, 8 тестів.
      Дві стелі, що можуть лише падати: **придатні, але не виміряні — 4**
      (це і є критерій допуску, порахований) і **небезпека звʼязків — 15**
      (форма Р28). Кількість непридатних (1166) НЕ під стелею: дві третини
      батчу — внутрішньоденне в печатці, збір якого триває за рішенням
      власника, і стеля там валила б кожен прогін.
      Читає закомічені звіти, а не паркет на 888 МБ, бо храповик, що
      пропускається в CI, — це не храповик (#261). Єдина перевірка, якій
      справді потрібен батч, винесена окремо, і її причина пропуску називає,
      що саме лишилось неперевіреним. Плюс тест, що лічильники ловлять
      власний випадок — без нього фільтр, який перестав збігатися, проходив
      би всі стелі.

- [ ] **Умова Р27 як четверті ворота, до першого зібраного бара:** кандидат
      вартий збору, якщо його брутто Шарпа перевищує чесний поріг НА
      ГОРИЗОНТІ, який тертя дозволяє.

### 1.2b Нетто-тест — новий крок, якого тут не було

**Заведено 03.09.** Пункт 1.2 закінчувався списком ознак, що «несуть
інформацію». Виявилось, що цього недостатньо навіть як проміжного результату:
єдина ознака, яка пройшла 1.2, дає брутто Шарпу 2.24 і нетто **−4.35**, бо
тертя коштує 27.6% річних при щоденному обороті.

Витрат не було **в жодному** кроці цієї карти. Три фільтри 1.3 — усі про
інформацію, жоден про ціну виконання.

- [ ] **Кожен кандидат із 1.2 проходить розгортку горизонту утримання** з
      тертям на ОБИДВІ ноги, перш ніж на нього витрачається щось іще.
      `scripts/diagnostics/does_lower_turnover_save_it.py`. Дешево, не потребує
      оцінки широти, дає пряму відповідь.
- [ ] **Витрати стають фільтром 4 у 1.3** — і застосовувати його треба
      ПЕРШИМ, бо він найдешевший і вбиває найбільше. Комісія у частках =
      `0.0035 / ціна`, тож дешева акція дорожча в торгівлі: $20 коштує
      1.75 б.п. за сторону, $230 — 0.15 б.п.
- [ ] **Умова приймання нового імені у всесвіт — дві, не одна:** низька
      кореляція з наявними І дешевизна виконання. Розширення підіймає і
      широту, і кількість угод; те, що дає Шарпу, її ж і оплачує.

**Чому це стоїть перед 1.3, а не після.** Просіювати сотні ідей Джеміні за
трьома інформаційними фільтрами, коли витрати зʼїдають навіть те, що вже
пройшло всі перевірки, — це впорядкувати чергу до зачинених дверей.

---

### 1.3 Ідеї Джеміні — просіяти, не приймати

Багато ідей, різної якості. Пропускати через три фільтри, кожен дешевий:

1. **Чи це не наслідок ціни?** Якщо величина — перетворення ціни, вона не випереджає.
2. **Чи є в неї крос-секційна варіація?** Ринковий ряд, однаковий для всіх тікерів,
   у ранжуванні дає рівно нуль. Це вбиває більшість «макро-ідей» одразу.
3. **Чи є в неї чесна дата доступності?** Без `filed`/`released` окремо від
   `period_end` ознака читає майбутнє. Уже ловили: ворота читали майбутнє, а T10YIE
   і T5YIE досі падають на поточну версію, тобто їхня доступність — дата збору.

Що проходить усі три — міряти звітом із 1.2. Що не проходить — записувати з причиною,
щоб не повертатись.

### 1.4 Агенти — після, і не «замість»

Твоє власне формулювання лишається правильним: агент споживає те, що дає конвеєр,
і на ненадійних ознаках він підсилює шум, а не сигнал.

**Але один виняток є.** Агент корисний **уже зараз** для задач судження, а не сигналу:
просіювання ідей (1.3), читання звітності, класифікація подій. Це текстова робота,
де немає ризику підсилити шумну ознаку.

- [ ] Агенти на **судження** — можна паралельно.
- [ ] Агенти на **рішення про угоду** — тільки після 1.2.

---

## 2. Що додати до списку, чого не було названо

Пункти, які я вважаю пропущеними й важливими.

- [ ] **Модель витрат.** Жоден звіт не рахує обіг і прослизання. Ознака може пройти
      IC і померти на комісії. Без цього всі числа — верхня межа, а не результат.
- [ ] **Пасивний бенчмарк у кожному звіті.** Пасивні 4.44% річних перемагали все, що
      ми будували. Результат без цього рядка нічого не означає.
- [ ] **Точка-в-часі як храповик.** Кожна нова ознака має мати мітку доступності, і
      контрактний тест має падати, якщо її немає. Ловили це вже тричі вручну.
- [ ] **Дедуплікація.** 430 ознак, **114 ефективних вимірів**. Надлишковість виміряна,
      але з неї не зроблено висновку: більшість ознак повторюють одна одну.
- [ ] **Контекст окремо від навчання.** Твоя думка, і вона правильна: ознаки можуть
      бути непридатні для передбачення, але цінні як **опис умов**, за яких отримано
      результат. Це окремий вихід конвеєра, а не вхід моделі. Розділити явно.
- [ ] **Чекпойнти скрізь, де крок довший за годину** (#122). Двічі за тиждень готова
      робота гинула на останньому кроці. Це форма, а не збіг.
- [ ] **Ніколи не діагностувати за числом, не перевіривши, як воно вимірюється**
      (#123). Найдорожча помилка дня: чотири правки за показником, який за побудовою
      не міг показати проблему.

---

## 3. Де що лежить

| файл | призначення |
|---|---|
| `REGISTER.md` | журнал: що сталося, що виміряно, що виправлено. Не план. |
| `ROADMAP.md` | **цей файл** — єдиний упорядкований список роботи |
| `WORKING_METHOD.md` | метод: як перевіряємо, що вважаємо доказом |
| `RESEARCH_IDEAS.md` | сировина: неперевірені ідеї до просіювання за 1.3 |

---

## 4. Каркас і рівні — і що з цього треба вирішити зараз

Задум власника: 110 відомих тікерів як **каркас**. Домогтися, щоб усе рахувалось
правильно — навіть без прибутку — плюс агент, який чесно каже «ринок такий-то, сиди
на місці». Потім спускатися глибше: малі компанії, водоканали, недооцінені за Гремом.

**Що в цьому правильно.** Ліквідні імена — найкращий полігон для перевірки
*правильності*: дані найчистіші, і якщо конвеєр бреше, це видно без суперечки з даними.
І «сиди на місці», сказане вчасно, — реальна цінність.

**Три хиби:**

1. **Не переноситься автоматично.** На малих іменах проблеми не менші, а **інші**:
   делістинг, застійні ціни, спред, нерегулярна звітність. Каркас підтверджує
   сантехніку, не статистику.
2. **Агент режиму міряється як прогноз, не як опис.** Мітка має **випереджати** те,
   що описує, інакше це зведення погоди після дощу. Рівно цю пастку вже ловили у
   воротах: опонент читав майбутнє й тому виглядав розумним.
3. **Грем — не глибший рівень, а другий прилад.** Багаторічні баланси, мертві
   компанії, горизонт у роки. Спільним лишається шар збору й точка-в-часі, не ознаки
   й не моделі.

### Рішення, яке не можна відкладати

- [ ] **Зберігати компанії, що зникають** — делістинг, поглинання, банкрутство —
      **починаючи з зараз**.

> Даних, яких не збираєш сьогодні, завтра не існує. Історію виживших можна отримати
> будь-коли, історію мертвих — ніколи заднім числом.

Без цього будь-який майбутній тест вартісної стратегії покаже чудовий результат, бо у
вибірці лишаться самі вижилі. Це найпоширеніша причина, чому такі стратегії прекрасні
на папері й нікчемні в житті.

Це вже почалося: в `assets.yaml` записано, що SQ і BLOCK перестали резолвитись на
Yahoo — і їх просто прибрали. Тобто зникнення вже трапляється, і ми його не зберігаємо.

---

## 5. Психологія — і єдине місце, де вона вимірювана

Теза власника: світові процеси здебільшого психологія, не лише економіка; на рівні
Nvidia один контекст, на рівні штатного водоканалу інший, але при розумінні
поверхневого плюс локального аналізу психологія стає зрозумілішою.

**Це правильно, і сильніше, ніж звучить.** Клас випереджальних величин із розділу 0 —
позиціонування, увага, вимушені дії, перегляд очікувань — **і є психологія**, названа
рахунковими іменами. Дві мови для одного твердження.

**Небезпека.** «Психологія» пояснює все заднім числом і не забороняє нічого наперед.
Будь-який рух можна назвати страхом або жадібністю після того, як він стався. Теорія,
яку неможливо спростувати, непридатна — не хибна, а порожня.

> **Фільтр для будь-якого твердження про психологію: яке число зрушилося б першим,
> і коли я міг би його побачити?** Немає такого числа — це наратив, не ознака.

**Хиба практична:** розуміння контексту водоканалу добре читаною людиною не
масштабується і не перевіряється. Для тисяч малих компаній локальні новини по кожній
не прочитати. Далі або система витягує локальний контекст **механічно** — важка задача,
яку треба ставити явно, — або стратегія тихо стає «довіряй чуттю». Друге нормально для
людини й не є системою.

**Переформулювання, яке об'єднує це з рештою:** психологія входить у систему в одному
місці, де її видно — **у розриві між тим, що вже відомо, і тим, що вже враховано в
ціні**. Увага, позиціонування й перегляд оцінок міряють саме цей розрив. Тому це не
окремий шар зверху, а причина, чому розділ 0 узагалі працює.

### З цього — вимога до схеми

Єдина схема для Nvidia й лісопилки правильна, але недостатня: **мовчання означає
різне**. Відсутній рядок у Nvidia — збій; у лісопилки — факт, що не було угод. Схема,
яка заповнить обидві порожнечі однаково, дасть ознаку, що міряє припущення, а не ринок:
ціна тритижневої давнини як «поточна» дає ідеально низьку волатильність і фальшивий
сигнал.

- [ ] Кожне значення несе **дату спостереження, дату доступності й міру надійності**
      (свіжість, ліквідність, джерело). Тоді обидва живуть в одній таблиці, а мовчання
      не читається як інформація.
- [ ] Кожне джерело приземляється як *сутність, коли спостережено, коли доступно,
      значення, звідки*. Саме це робить додавання шарів дешевим: новий шар ніколи не
      змінює попередні.

---

## 6. Зворотний аналіз, події та правило підрахунку

Теза власника: не прогнозувати, а **моделювати умови, за яких люди прийняли рішення**.
Часові ознаки, локальні новини, соціальні події. Приклад: Клівленд виграє титул НБА →
ейфорія → більше витрат і, можливо, інвестицій.

**Правильне.** Перехід від «передбачити в середньому» до «виміряти всередині класу
подій» — законний і давно робочий. Ознака, мертва в середньому по всіх днях, може
працювати у вузькому вікні. Грем теж шукав **кон'юнкцію умов**, а не одне P/E.
Машинне читання локальних новин по тисячах компаній справді стало дешевим. Ефект
спортивних результатів на ринок — реальна дослідницька лінія, не вигадка.

**Небезпека — арифметична.** Тридцять команд, один чемпіон на рік, десять років — це
**десять подій на всю лігу**, одна-дві на місто. З десяти спостережень не вимірюється
нічого, хоч би яким був конвеєр.

**І гірше: чим креативніший пошук, тим гарантованіший хибний результат.** Пущена
перебирати спортивні результати, салюти, погоду й локальні новини, машина **обов'язково**
знайде «Клівленд виграв → місцеві банки зросли» з гарною значущістю. При тисячах
кандидатів такі збіги неминучі. Та сама властивість, що робить напрям захопливим —
необмежений простір гіпотез — робить його нездатним себе перевірити. Пор. розділ 4
критики: одна ознака з 430 дала 5.3 SE, і це був очікуваний максимум за випадковості.

> **Правило: клас подій придатний до дослідження лише якщо подій досить, щоб їх
> виміряти. Рахувати кількість подій ПЕРЕД тим, як будувати дослідження.**
> Титул Клівленда: 1–2 випадки → не досліджується, хоч би яким правдоподібним був
> механізм. Дрейф після звітності: ~440 подій на рік при 110 іменах → досліджується.

**Валідна версія:** небагато обґрунтованих класів із сотнями випадків — сюрпризи у
звітності, включення в індекси, кластерні купівлі інсайдерів, зміни прогнозів компанії,
пробої річних максимумів. Та сама психологія, але з числом, датою і достатньою
кількістю повторень.

- [ ] Локальні новини — це насамперед **зіставлення сутностей**, а не тональність.
      Зв'язати «лісопилку в Теннессі» з тікером у потоці новин важче, ніж оцінити
      настрій. Ставити як окрему задачу, інакше вона тихо провалиться всередині іншої.

---

## 7. Два прилади: статистичний і сценарний

Уточнення власника: йдеться не про екзотичні події, а про **тригери й причинні
ланцюги**. Приклад: TSMC будує завод у Феніксі. Контекст — Тайвань і можлива війна,
нестача води в Арізоні, чи вистачить інженерів у США, чи завод узагалі запрацює. Якщо
Тайвань стане «як Гонконг», завод може втратити значення. Це моделювання варіантів.

**Це інший прилад, а не глибший рівень того самого.**

| | статистичний | сценарний |
|---|---|---|
| вхід | багато схожих випадків | одна унікальна ситуація |
| метод | оцінка ефекту | перелік механізмів і гілок |
| потребує | кількості спостережень | явних припущень |
| перевіряється | out-of-sample | **калібруванням прогнозів** |

TSMC в Арізоні — N = 1. Backtest неможливий у принципі; це природа задачі, не вада.

**Але сценарне мислення перевіряється — просто одиниця виміру інша: не подія, а
прогноз.** Заявляється наперед «ймовірність X до дати D = 0.6» з критерієм розв'язання;
через сотні таких заяв перевіряється калібрування — з того, чому давали 60%,
справдилось приблизно 60%? Працює саме з унікальними подіями, бо агрегує по прогнозах.

**Тут агенти на своєму місці.** «Агенти після конвеєра» (розділ 1.4) стосувалось
торгових рішень. Для сценарного шару агент — правильний інструмент: перелічити
механізми, знайти залежності, тримати сотні сутностей, оновлювати стан.

**Небезпека більша, ніж у статистиці.** Мовна модель згенерує правдоподібний причинний
ланцюг для чого завгодно — зв'язне мовлення і є те, що вона вміє. Без записаних наперед
ймовірностей це генератор наративів: та сама «погода після дощу», автоматизована й
набагато переконливіша.

> **Дисципліна: кожне сценарне твердження має ймовірність, дату й критерій розв'язання.
> Твердження, яке неможливо розв'язати, не приймається.**
> «Тайвань може стати як Гонконг» — ні. «Ймовірність, що TSMC Arizona відвантажить
> понад X пластин/міс до кінця 2027 — 0.45» — так.

- [ ] Журнал прогнозів: твердження, ймовірність, дата, критерій, хто/що заявило.
- [ ] Періодична перевірка калібрування. Без неї сценарний шар не має статусу.

---

## 8. Як подати контекст машині

Питання власника: він читає багато новин без індикаторів і порівнянь, і вони формують
контекст — суб'єктивний, місцями хибний. Хотів «детальний лічильник», який рахує не
просто згадки «чіпів», а згадки **в контексті**. Не знав, як це реалізувати.

**Змішані три різні речі. Розділені — стають простими.**

| | що це | як рахується |
|---|---|---|
| **увага** | скільки про це говорять | згадки, пошук, перегляди Wikipedia — вже вміємо |
| **стан** | що зараз істинне про сутність | **не рахується**: база фактів із датами |
| **очікування** | у що вірять щодо майбутнього | оцінки аналітиків, прогнози компаній, ф'ючерсні криві, власні прогнози агента (розділ 7) |

Стан лягає в ті самі п'ять полів, що й будь-яке джерело (розділ 5): *сутність, атрибут,
значення, коли спостережено, коли доступно, джерело, надійність*.

### Правило, яке відповідає на питання про лічильник

> **Константи — це контекст для людини. Зміни — це дані для машини.**

«В Арізоні мало води» — константа: не змінюється, однакова для всіх, уже врахована.
Моделі вона не дає нічого. «**Такого-то числа** водну квоту заводу переглянули вниз» —
подія з датою, і вона працює.

**Тому контекстний лічильник — це лічильник змін типізованих фактів, а не слів.**
Не «скільки разів згадали чіпи», а «скільки понижень планів потужності цього кварталу».
Рахується так само легко й означає щось.

### Як будувати

- [ ] Агент читає новини й **пропонує факти в схему**, ніколи у вільний текст. Якщо
      твердження не вкладається в *сутність, атрибут, значення, дата* — не записується.
      Це і є автоматичний фільтр проти наративу: у наративу немає атрибута й дати.
- [ ] Кожен факт несе джерело й надійність; базу вибірково перевіряти на точність
      витягування — так само, як прогнози перевіряються калібруванням.
- [ ] Ознаки будуються з **різниць** цієї бази, не з її рівнів.

---

## 9. «Чому» — граф залежностей, а не пояснення

Питання власника: окрім датованих фактів має бути **загальний контекст** — чому Тайвань
узагалі будує в Арізоні, яка геополітика; чому влада умовного Огайо раптом штовхає
інвестиції. Як подати машині сам **корінь логіки**.

**Уточнення правила з розділу 8.** «Константи для людини, зміни для машини» вірне щодо
**ознак**. Але загальний контекст потрібен — не як ознака, а як **структура, що
вирішує, які зміни мають значення**. Той самий рівень води означає різне залежно від
того, чи щось від нього залежить. Геополітика не дає числа — вона каже, **які
залежності існують**.

**Що таке «чому» технічно.** «Чому Тайвань будує в Арізоні» розкладається на факти, які
вже вміємо зберігати: концентрація виробництва на одному острові, субсидії, близькість
до замовників. Кожен — сутність, атрибут, значення, дата. Отже «чому» не є додатковою
інформацією; це **граф залежностей між наявними фактами**.

> **Операційна функція «чому» одна: воно каже, що саме зруйнує висновок.**

Аналітик, який розуміє причину, знає, за чим стежити. Це і є розуміння в машинному
вигляді — не пояснення, а перелік спостережень, здатних змінити твердження.

### Форма запису сценарного твердження (доповнює розділ 7)

| поле | приклад |
|---|---|
| ймовірність, дата, критерій розв'язання | розділ 7 |
| від чого залежить | субсидії, водна квота, наявність інженерів |
| у який бік тисне | скасування субсидій → вниз |
| **що я побачу, якщо це зміниться** | конкретний спостережуваний факт із джерелом |

Останній рядок головний. **Твердження, для якого не можна назвати спостереження,
здатне його спростувати, не приймається** — так само, як твердження без дати.

Це замикає конструкцію: факти — вузли (розділ 8), очікування — твердження над графом
(розділ 7), а граф **сам породжує лічильники**: не «що рахувати», а «за чим стежити».

### Попередження про збіг факторів

Кілька слабких факторів разом людина сприймає як підсилення доказу. Це найнадійніший
спосіб отримати хибну впевненість: кількість комбінацій росте так швидко, що якийсь
збіг знайдеться завжди (пор. розділ 6). Валідна форма — збіг як **генератор гіпотез**,
де кожен елемент має власну дату й спостережуваний тригер. Граф робить це автоматично:
не можеш назвати ребро й спостереження — воно не входить.

---

## 10. Актори, стимули й календар — і як виразити «це театр»

Уточнення власника: йдеться **не про купівлю/продаж**, а про накопичення й опрацювання
контексту. Хочеться, щоб модель розуміла «в них вибори, катастрофа, відкриття об'єкта —
вони можуть діяти», і тримала графи ймовірностей подій.

**Форма, у якій це реалізується.** Твердження «в них вибори, вони можуть діяти»
розкладається на п'ять частин, кожна зберігана й перевірна:

| частина | приклад |
|---|---|
| **актор** | губернатор штату, рада компанії, ФРС, CEO |
| **календар** | вибори, дати звітів, межі повноважень, погашення боргу, дедлайни дозволів |
| **стимул** | перед виборами чинному потрібні видимі перемоги |
| **репертуар** | скінченний набір дій: оголосити субсидію, прискорити дозвіл, відкласти погану новину |
| **спостережуване** | пресреліз, подана заявка, рядок у бюджеті |

**Календар — ключ.** Він відомий наперед і є тим самим класом «вимушених майбутніх
подій» із розділу 0: дешевий у зборі й випереджальний за побудовою.

### Як виразити «це політична гра, варто почекати»

> **Дія, передбачувана зі стимулу актора, не несе інформації.
> Дія проти стимулу несе її багато.**

Губернатор оголошує інвестиції перед виборами — нічого нового, це передбачалось із
календаря. Губернатор оголошує **погану** новину перед виборами — інформації багато, бо
він робить те, що йому шкодить.

Саме це людина називає «театром», і воно рахується: **несподіванка = відхилення дії від
того, що передбачав стимул**. Оцінювати щирість не потрібно — досить календаря й
репертуару.

- [ ] Почати вузько, з акторів із безкоштовним календарем: компанії (дати звітів і
      прогнозів), ФРС (засідання). Штати й дозволи — потім, це новий збір.
- [ ] Шар видає датовані очікування → перевіряється калібруванням (розділ 7). Тобто
      накопичення контексту стає **вимірюваним**, а не просто накопиченням.

---

## 11. Не все стає показником

Питання: чи кодувати цей абстрактний шар у показниках?

**Ні.** Уся попередня вимога «зробити рахунковим» легко читається як «усе перетворити на
число» — і це головна пастка.

| спосіб запису | для чого | приклад |
|---|---|---|
| **числа** (ознаки) | те, що повторюється сотні разів і усереднюється | сюрприз у звітності, ширина переглядів оцінок |
| **структура** (факти, календарі, ребра графа) | те, про що **міркують** | цільовий запуск заводу; «залежить від субсидій» |
| **датовані ймовірності** | унікальні ситуації | «0.45, що відвантажать понад X до кінця 2027» |

Спроба стиснути «політика Огайо» в число дає «оцінку політичного ризику 0.7», з якої
**неможливо дізнатись, що її змінить**: втрачено залежність і тригер, тобто саме те,
заради чого будувався граф. Число виглядає науково і не робить нічого.

> **Числа — для того, що усереднюють. Структура — для того, про що міркують.**

**Місце з'єднання:** база фактів сама породжує ознаки там, де тип факту повторюється
достатньо часто. «Прогноз компанії переглянуто вниз» — сотні разів на рік, це число.
«Водну квоту заводу переглянуто» — двічі, лишається структурою.

Порядок один для всього: **усе приземляється як структура, а числа випадають самі
там, де кількість подій дозволяє** (правило підрахунку, розділ 6).

Звідси й відповідь на «щоб машина розуміла»: розуміти в людському сенсі їй не треба.
Треба зберігати типізовані факти, тримати твердження з тригерами й діставати їх, коли
доречно. **Розуміння лежить у структурі, а не в оцінці.**

---

## 12. Відкрите: 0.73% найдавніших рядків мають хибні технічні ознаки

Перевірка 2026-08-28: збережена `SMA_20_1d` збігається з перерахунком на
**99.4%** рядків, кадр монотонний за датою — **ціновий блок здоровий**, і
відсутність сигналу в ньому справжня, а не артефакт.

Але **47–54 перші рядки кожного тікера** розходяться, у всіх перевірених
дванадцяти, завжди з індексу 0. Разом 0.73%. Значення спадають (302.8 → 286.5
→ 270.1 для AAPL, де справжня ціна 0.18) — підпис вікна, засіяного чужими
значеннями близько 320, які поступово вимиваються.

**Причина не знайдена.** Групування в `technical_analysis_enricher` коректне:
`df.groupby('ticker', sort=False)`, кожне ім'я окремо. Отже забруднення
приходить не звідти.

- [ ] Знайти джерело. Перевірити, чи не рахується `SMA_20` десь іще, і чи
      збігається колонка `close` на момент збагачення з тією, що в батчі.
- [ ] Ці рядки — найдавніші, тобто цілком у навчальному вікні.

Не блокує прогін: 0.73% проти 99.4% підтверджених.

---

## 13. Передпольотна перевірка: як не гнати півдня даремно

`scripts/diagnostics/batch_invariants.py` — шість перевірок, і кожна це
дефект, який ми справді відвантажили. Усі вони видно в готових даних за
секунди, а дізнавались ми про них через вісім, одинадцять або дванадцять
годин.

**Ключ у тому, що перший чекпойнт з'являється на 50-й хвилині прогону.**

```bash
python scripts/diagnostics/batch_invariants.py data/checkpoints/enriched/enriched_1d.parquet
```

Поганий прогін закінчується там, а не на дванадцятій годині. Код виходу
ненульовий при будь-якому провалі, тож перевірку можна ставити воротами.

| перевірка | дефект, який ловить |
|---|---|
| рядки впорядковані за часом | 30 років заглядання з порядку рядків (#128) |
| макро однакове для всіх імен на дату | мітка когорти під виглядом промвиробництва (#125) |
| стан контексту однаковий для всіх | чемпіон, розлитий по рядках |
| жодна колонка не є переважно своєю медіаною | 70% вигаданих значень (#125) |
| індикатори збігаються з перерахунком | найдешевша перевірка, яку не робили місяцями |
| ознаки змінюються під час навчання | мертві колонки (#126) |

Перевірено на завідомо зіпсованому чекпойнті: **4 провали з 6**, і два
здорові пройшли. Читає блоками по 150 колонок, щоб не тягнути кадр у пам'ять.

**Правило:** будь-який новий клас дефекту, знайдений руками, стає сьомою
перевіркою. Інакше наступного разу його теж шукатимуть півдня.

---

## 14. Макро: глибина є, її просто не просили

Виміряно проти живого API 2026-08-28, запит від 1996 року:

| ряд | із версіями | без версій |
|---|---|---|
| `DGS10` (щоденний) | **відмова 400**: «5097 vintage dates… exceeds the maximum» | 7 998 дат |
| `CPIAUCSL` (місячний) | 1 766 рядків, 367 дат, **385 версій** | 367 |
| `GDP` (квартальний) | 1 231 рядок, 122 дати, **361 версія** | 122 |

**Відмова не залежить від глибини спостережень** — вікно версій задане як
1776→9999 у будь-якому разі, тож щоденний ряд відхиляють навіть на двох роках.
Отже кожен щоденний запит падав і тихо відкочувався на поточну версію, а
дворічне вікно, схоже, і було спробою це обійти.

**Вирішує переглядуваність.** Дохідність облігацій, закриття VIX чи курс за
конкретний день — це спостереження, не оцінка; FRED їх не переглядає. CPI, GDP
і зайнятість — оцінки, які переглядають роками, і для них версії це весь сенс.

Зроблено: `period: 2y → 30y`, а щоденні ринкові ряди просяться **без вікна
версій** (`UNREVISED_DAILY_SERIES`) — запит не надсилається туди, де відповідь
відома наперед.

### Що з цього випливає для ролі макро

Макроряд — одне число на всю економіку. У **ранжуванні імен** його внесок рівно
нуль, хоч би яким випереджальним він був: він не розрізняє AAPL і XOM. Працює
у двох інших ролях:

- **режим / таймінг** — «чи взагалі час бути в ринку», тобто агент «сиди на місці»;
- **взаємодія** — макро × чутливість конкретного імені, і **це вже різне** для різних імен.

І більшість макро за побудовою **відстає**: GDP та CPI вимірюють минуле.
Випереджають лише ті, що відображають очікування — `T10Y2Y`, `T10Y3M`,
`BAMLH0A0HYM2`, `T5YIE`, `ICSA`, `UMCSENT`.

- [ ] **Відкрите, знайдено поруч:** `available_at` перевіряється стадією 2, але
      **у злитті не використовується** — воно йде за датою спостереження. Тобто
      точка-в-часі валідується й не застосовується.

---

## 15. Відкрите: стан ринкової колонки рахується з порогом на кожен тікер

Перевірка інваріантів на свіжому чекпойнті 2026-08-28 (12:12, сімдесят п'ята
хвилина прогону) підтвердила головне: **`macro agrees across tickers` — 0 з 20**,
було 19 з 20. Виправлення порядку рядків працює на реальних даних.

Але похідні стани — ні:

| | розбіжність між іменами |
|---|---|
| `FRED_BAMLC0A0CM_15m` (значення) | **0.0%** |
| `state_FRED_BAMLC0A0CM_15m` (стан) | **50% дат** |

**Причина.** Стан це `returns > threshold ? 1 : returns < -threshold ? -1 : 0`,
а поріг рахується `groupby`-перетворенням **на кожен тікер**. Значення ринкової
колонки однакові для всіх імен, пороги — ні, бо історії різної довжини. Звідси
розбіжність на рідкісних спрацюваннях.

**Масштаб малий:** стан на 98% нульовий (0 трапляється 153 457 разів, ±1 разом
2 915), тож зачеплено близько 2% рядків.

**Не виправлено свідомо.** Зміна торкнеться того, як рахуються всі 615 колонок
стану — це рішення про сенс величини, а не усунення баги:

- [ ] Для **ринкової** колонки стан має бути один на дату — тобто поріг
      обчислюється глобально, а не на тікер.
- [ ] Для **поіменної** колонки поточна поведінка правильна.
- [ ] Отже потрібне розрізнення: чи колонка ринкова. `varies` зі звіту про
      випереджальність уже це міряє — воно й може бути критерієм.

Прогін 2026-08-28 не зупинявся: значення виправлені, стани зачіпають 2%, а
наступний прогін усе одно потрібен заради тридцятирічного макро.

---

## 16. Умовний аналіз: механізм є, вимірювання немає, а KNN шукає не там

Питання власника: чи можна міряти кореляцію **за умовами** — «зі ста
15-хвилинних свічок є така-то залежність при таких-то умовах». Чи є це в
проєкті й чи має сенс.

**Здійсненно — але не тим стовпцем, яким його пробували.**

| | `context_pattern_id` | `context_fingerprint` |
|---|---|---|
| різних значень | **682 035** | **2 027** |
| рядків на умову | 1.03 | **348** |
| умов із >1000 рядків | 0 | **166** |
| умов із >100 рядків | 5 | 675 |

`context_fingerprint` — стан **одного бара**, склеєний із 8 налаштованих
драйверів (SMA_200, SMA_20, ATR_14, волатильність…), кожен на три значення.
`context_pattern_id` — хеш **послідовності** з `pattern_length` таких
відбитків, тому його простір 6 561^k і кожен рядок унікальний за побудовою.

### Дефект, знайдений дорогою

- [ ] **`KNNSimilarityFinder` фільтрує за `context_pattern_id`** із порогом
      `min_regime_samples = 20`. При 1.03 рядка на патерн умова не виконується
      **ніколи**, тож він завжди відкочується на глобальний пошук. Механізм
      працює, пише в лог і не робить того, для чого написаний. Має фільтрувати
      за `context_fingerprint`.

### Чи має сенс міряти за умовами

Так, і це практична форма власної думки власника про «опис умов, за яких
отримано результат». Але з двома обмеженнями, які треба закласти **до**, а не
після:

1. **Множинні перевірки.** 166 умов × 430 ознак = **71 380 тестів**. Без
      поправки хибна знахідка гарантована — та сама пастка, що дала IC 5.3 SE,
      який виявився очікуваним максимумом за випадковості.
2. **Розмір умови видно поруч із числом.** IC без кількості рядків, на яких
      він порахований, не є результатом.

- [ ] Додати до `leading_feature_report` колонку «IC усередині умови» для
      **невеликого, заздалегідь оголошеного** набору умов — наприклад, топ-N
      відбитків за покриттям — і завжди друкувати розмір умови поруч.
- [ ] Умови **не шукати** під результат: підбір умови під ознаку це підганяння,
      і це найнадійніший спосіб отримати гарний бектест, що не працює.

**Про «сто свічок».** Сто барів дають сто точок для оцінки кореляції — оцінка
дуже шумна, а сусідні блоки перекриваються за режимом, тож незалежних серед них
значно менше, ніж здається. Тому міряти варто по **умовах** (де вибірка
збирається з усієї історії), а не по блоках підряд.

---

## 17. Відкрите: одинадцять тікерів відстають на одну публікацію макро

Перевірка інваріантів на денному чекпойнті 2026-08-28 після всіх виправлень:

| перевірка | стан |
|---|---|
| рядки впорядковані за часом | ✅ |
| індикатори збігаються з перерахунком | ✅ 99.3% |
| макро однакове для всіх імен | ⚠️ 12 з 20 |
| стан контексту | ⚠️ 6 (відомий пункт §15) |
| колонки = медіані | ⚠️ 13 — сентимент і новини, покриття 1.75% |
| мертві в навчанні | ⚠️ 146 — дворічне макро, у конфігу вже 30y |

**Залишкова розбіжність макро — виміряна й вузька:**

- **344 дати з ~7 500 (4.6%)**, усі між 2024-08-02 і 2026-04-30, тобто **цілком
  усередині** дворічного покриття збирача;
- на 2025-08-20: 106 тікерів мають CPI 323.291, **чотири** — 322.169, тобто
  попередню публікацію;
- **мітка часу однакова для всіх — 00:00:00**, тож це не різниця моменту бару;
- у меншості бувають **лише 11 тікерів зі 110**, і ті самі: CHTR (99 разів),
  COIN (83), RIOT (83), AMZN (82), QQQ (82), WELL (81), APD (41), SO (40).

**Отже причина структурна, у даних цих імен**, а не в порядку рядків (той
виправлено: на 15-хвилинному кадрі 0 з 20 розбіжних, і денні розбіжності
зникають поза вікном покриття).

- [ ] Гіпотези до перевірки: пропуски барів навколо дат публікації; або
      розбіжність часової зони, що зсуває їхні рядки на кілька годин відносно
      моменту релізу. **Не перевірено — перевіряти на цих 11 іменах.**
- [ ] Масштаб: ~0.5% рядків. На порядок менше за виправлене сьогодні (98% дат,
      70% значень), тож не блокує прогін.

---

## 18. Умовний пошук тенденцій: конструкція і де вона працює вже

Задум власника: збираємо багато станів і аналізаторів, а **пошук** ведемо
батчами — «зі ста ранкових 15-хвилинних свічок вісімдесят поводяться так за
такої волатильності». Шукаємо **тенденцію**, а не п'ять-сім співпадінь.
Приклад умови: перша половина понеділка, новин певного типу не було, потрясінь
немає.

**Задум правильний.** Це умовний аналіз із наперед оголошеними умовами, і
приклад із понеділком саме того типу, що треба: умова **названа заздалегідь і
має механізм** (люди повертаються з вихідних), а не знайдена перебором.

### Мінімальний відсоток не задається — він виводиться

Для частки похибка це `√(p(1−p)/n)`:

| незалежних випадків | похибка | що означає 60% |
|---|---|---|
| 100 | 5.0% | два стандартні відхилення — слабко |
| 1 000 | 1.6% | шість |
| 3 000 | 0.9% | одинадцять |

Тобто питання «який мінімальний відсоток» правильно звучить як **«скільки в
мене незалежних випадків»**, і поріг рахується сам.

### Де це вимірюється вже сьогодні, а де ні

Виміряно 2026-08-28:

| тип умови | кадр | незалежних спостережень | вердикт |
|---|---|---|---|
| день тижня, сезонність | денний, 1996–2026 | **≈1 560 понеділків** | **можна зараз** |
| частина дня, ранок/вечір | 15m: 2026-06-09..08-27 | **≈11 понеділків** | ні |
| режим волатильності | денний, 30 років | тисячі | можна |
| «не було новин такого типу» | — | покриття новин 1.75% | ні |

15-хвилинний кадр має **1 421 бар на тікер, тобто ~55 торгових днів**, бо
`intraday_max_days: 60` — це межа Yahoo, а не наш вибір. Поглибити конфігом
неможливо: лише накопичувати (~4 понеділки на місяць роботи) або купувати
історію.

**Тобто приклад із понеділком вимірюваний — але на денних барах.**

### Конструкція

- [ ] **Умови оголошуються наперед і їх мало.** Не 2 027 відбитків: щось на
      кшталт день тижня (5) × терциль волатильності (3) × напрям ринку (3) = 45.
- [ ] **Наслідок фіксований**: дохідність через N барів, обраний до виміру.
- [ ] **Статистика друкується з n і похибкою.** IC чи частка без кількості
      рядків за нею — не результат.
- [ ] **Поправка на множинні перевірки** (FDR), і кількість перевірених умов
      друкується поруч.
- [ ] **Підтвердження на відкладеному періоді.** Умова, знайдена на навчальній
      частині, має вижити на перевірочній — це найдешевший спосіб відрізнити
      залежність від збігу.
- [ ] Умови **не підбирати під результат**: підбір умови це підганяння, і саме
      так виходить гарний бектест, що не працює.

### Що виправити, щоб це стало можливим

- [ ] `KNNSimilarityFinder` фільтрує за `context_pattern_id` (1.03 рядка на
      патерн) замість `context_fingerprint` (348 рядків) — див. §16.
- [ ] Додати умовний вимір у `leading_feature_report`: IC усередині умови,
      з розміром умови поруч.

---

## 19. Перші умовні виміри: ціна дає премію за ризик, позиціонування мовчить

Інструмент: `scripts/diagnostics/conditional_pattern_report.py`. Умова на барі
`t`, дохідність за наступні N барів — тобто «контекст до, результат потім», без
заглядання наперед.

### Дорогою довелося викинути дві власні метрики

| версія | що насправді міряла |
|---|---|
| `P(верхні 10% дохідності)` | **волатильність**: кореляція з розкидом **+0.889**; «205 із 368 знахідок» |
| середня проти нуля | **дрейф ринку**: база +0.326% за 5 днів, тож майже все позитивне |
| **надлишок над базовою** | те, що треба |

### Результати на денному кадрі, горизонт 5 барів

**Умови з ціни** (`context_fingerprint`: 6 із 8 драйверів — SMA, ATR, RSI, obv):

- **15 із 368** переживають поправку Бенджаміні–Хохберга **і** повторюються на
  відкладеній частині;
- найсильніша: `-1|-1|0|0|-1|-1|0|` — 4 254 рядки, +0.985% проти базових
  +0.327%, z = 6.21, на відкладеній +0.937%;
- **але розкид у виживших 6.9–8.0% проти 3.5–4.5% в решти**. Дохідність на
  одиницю ризику 0.142 проти ~0.07 — удвічі краще, і це може бути **платою за
  ризик**, а не неефективністю.

**Умови з позиціонування** (`state_cftc_sp500_ls_ratio`):

- **0 із 3** виживають. +1: n=1 924, +0.155%, z=−1.62. −1: n=1 697, +0.369%,
  z=0.48;
- **сила тесту:** при розкиді 4.65% і n≈1 900 похибка 0.106%, тож ефект близько
  **+0.32% за 5 днів** був би помічений. Менший — ні. Це «доказів немає», а не
  «ефекту немає».

### Що з цього випливає

- [ ] **Наступний вимір для цінових умов:** дохідність **на одиницю ризику**
      проти базової, а не дохідність. Це відрізнить сигнал від премії за ризик.
- [ ] **Випереджальні умови впираються в ту саму стіну**, що новини й макро:
      `state_cftc_*` ненульові на 2.2–3.4% рядків, `state_fund_price_to_book` на
      10.7%. Подій мало, тож тести слабкі.
- [ ] Відбиток варто розширити неціновими драйверами — але два наявні
      (`state_nlp_sentiment_score`, `state_macro_composite_score`) саме ті, що
      виміряні зламаними (98% нулів і 87% медіани). Спершу дані, потім умови.

---

## 20. Ролі показників: таксономія і чому правильність іде першою

Погоджено з власником 2026-08-28.

**Спершу правильність конвеєра — навіть якщо випереджальних показників не
виявиться взагалі.** Не з обережності: інший порядок не дає знання. Місяцями
мірялась випереджальність макроознак, у яких 70% значень були медіаною колонки
з майбутнім усередині (#125). Висновки того періоду не помилкові, а **порожні**
— міряли не те, що думали. Зворотне неправда: щойно дані справжні, розкладання
за ролями коштує вечір.

**Роль не є властивістю показника — вона результат виміру**, і один показник
може мати кілька ролей одночасно.

| роль | що робить | чим доводиться | приклад |
|---|---|---|---|
| **зламаний** | не міряє нічого реального | `batch_invariants.py`, **першим** | FRED із 70% медіани; стан чемпіона по рядках |
| **випереджальний** | інформація про майбутнє понад ціну | IC точка-в-часі, всередині тікера, знак OOS | **жодного підтвердженого** |
| **умова** | не передбачає, але змінює залежність | надлишок над базовою + FDR + повторення | 15 цінових відбитків (§19) |
| **взаємодія** | нуль у ранжуванні, працює × чутливість | IC добутку | макро — **не перевіряли** |
| **опис контексту** | не входить у модель, фіксує умови | не перевіряється передбаченням | ідея власника (§2) |
| **ніщо** | провалює все | — | більшість із 430 |

**Одна величина, три ролі.** Волатильність провалюється як предиктор напрямку,
працює як умова, і водночас є фактором ризику, через який та сама умова дає
вищу дохідність. Плутанина між цими трьома — джерело хибних знахідок, і саме на
ній я спіткнувся тричі за один вечір (§19).

**Станом на 2026-08-28: нуль підтверджених випереджальних показників.** Не тому
що їх немає — тому що досі мірялись або зламані дані, або не та величина. Це
перший день, коли інструменти дають числа, яким можна вірити.

### §17, доповнення: обидві гіпотези спростовані

Перевірено 2026-08-28 перед запуском:

- **пропуски барів — ні.** У «відсталих» їх стільки ж: AMZN 336, AAPL 329, і
  AAPL не відстає. CHTR 189, WELL 313 — розкид не корелює з відставанням;
- **часова зона — ні.** Мітки ідентичні **до наносекунди**:
  `int64 = 1755648000000000000` в AAPL і AMZN на 2025-08-20, а значення різні
  (323.291 проти 322.169);
- **це не відставання на одну публікацію, а стійкий якір.** AMZN тримає 322.169
  усі десять днів навколо релізу, AAPL — 323.291. Кожен тікер тягне **своє**
  спостереження вперед.

**Отже причина всередині злиття або заповнення, а не в даних імен.**

**Відкладено свідомо.** У кадрі лише **13 різних значень CPI** — наслідок
дворічного вікна збирача. Після `period: 30y` їх стане близько 360, щільність
зросте в тридцять разів, і механізм, що лишає тікери на різних якорях, може
поводитись інакше або зникнути. Розслідувати на розріджених даних, які за п'ять
годин буде замінено, — марна робота.

- [ ] Перевірити на батчі з тридцятирічним макро. Якщо збережеться — дивитись
      `_merge_with_duplicates` і `_fill_forward_in_time`, бо решту виключено.

---

## 21. Дефект кешу: фінгерпринт коду пишеться в кінці прогону

Знайдено 2026-08-28 о 20:44, коли запуск із тридцятирічним макро **пропустив
стадії 0–3** і за хвилину пішов пакувати старі дані.

**Механізм.** Кеш порівнює фінгерпринт коду з тим, що збережено поруч із
батчем. Але фінгерпринт **обчислюється й зберігається наприкінці** прогону:

| | час |
|---|---|
| прогін v24 стартував зі **старим** збирачем (`period: 2y`) | 10:57 |
| конфіг змінено на `30y` | ~15:00 |
| прогін записав фінгерпринт | **20:01** |

Тобто збережене значення описує код, якого під час збору **не існувало**. Кеш
чесно каже «код не змінювався з моменту збору» — і це неправда.

**Наслідок точний:** будь-яка правка, зроблена **під час** довгого прогону,
записується так, ніби вона в ньому діяла, і наступний прогін її не застосує.
За добу таких правок було вісім.

- [ ] Фінгерпринт коду має обчислюватись **на старті** стадії 0 і зберігатись
      разом із батчем — тоді він описує код, який справді збирав дані.
- [ ] Обхід на зараз: перед запуском після правок відсунути
      `raw_db_fingerprint.json`.

Це той самий клас, що й решта знайденого сьогодні: механізм працює, звітує про
успіх і робить не те, що обіцяє.

## 22. Стіна доступності: як виправлення випередження відкинуло історію

Виміряно 2026-08-28 на кадрі прогону v26, `enriched_1d.parquet`, 705 274 × 475.

**Спостереження, з якого все почалось.** Сторож повідомив 107 сталих у
навчальному вікні ознак на денному кадрі. Очікувалось, що після переходу
`period: 2y → 30y` число обвалиться зі 146 майже до нуля. Не обвалилось.

**Що показали дані.**

```
кадр          1996-08-26 → 2026-08-27,  межа навчання 2018-11-21
21 із 45 рядів FRED   перше значення липень 2024   у навчанні нуль рядків
DGS10                 555 спостережень з 2024-07-08   (в архіві FRED ~8 000 з 1996)
```

**Збір був правильний.** У лозі 45 запитів, усі з
`observation_start=1996-09-04`, жодного винятку.

**Механізм.** Вісімнадцять із двадцяти одного мілкого ряду — це точний склад
`UNREVISED_DAILY_SERIES`. Для них запит іде без вікна версій, FRED повертає
поточну версію і ставить `realtime_start` = дата запиту на **кожне**
спостереження. Ця колонка стає `available_at`. Тобто тридцять років історії
лежать у базі з позначкою «стали відомі 28 серпня 2026».

**Чому оцінка ціни була неправильна.** Коментар у коді оцінював втрату як
«близько дня випередження». Для щойно опублікованого спостереження це так.
Для **дозаповненої історії** — ні: запис 1996 року зі штампом 2026 запізнюється
на тридцять років, тож усередині навчального вікна не з'являється взагалі.
Одна й та сама операція коштує день на свіжих даних і всю історію на старих.

**Виправлення** назване в тому ж коментарі й не застосоване: для рядів, які
FRED не переглядає, доступність треба виводити з самих даних —
`available_at = дата спостереження + лаг публікації` — замість штампа запиту.

- [ ] Вивести доступність для `UNREVISED_DAILY_SERIES` з дати спостереження
- [ ] Перезібрати лише FRED і переграти макро-збагачення, без повного кадру
- [ ] Пояснити три ряди поза списком: `CAPUTLB50001SQ`, `INDPRO`, `RSAFS`
- [ ] Перевірити гіпотезу #131: чи не схлопує чистка версії довільно

**Клас той самий, що й у решти знайденого за добу:** механізм працює, звітує
про успіх і робить не те, що обіцяє. Різниця в тому, що цього разу механізм —
власне вчорашнє виправлення, і воно рухалось у правильний бік: краще чесна
відсутність даних, ніж фальшива доступність. Але ціну треба було виміряти, а
не оцінити з коментаря.

## 23. Два шари: приймання даних і перевірка варіантів

Рішення власника, 2026-08-28. Нижній шар збирає, чистить і **розмічає** дані.
Верхній моделює, критикує, відкидає варіанти. Вибір ознак і навчання —
похідні від верхнього шару, не окремий етап.

Поділ правильний і збігається з практикою: шар даних можна перевірити
інваріантами, шар варіантів — тільки статистикою.

**Умова, без якої другий шар шкодить більше, ніж дає.** Кожен варіант, який
він здатен згенерувати, є перевіркою. Десять ознак × п'ять горизонтів × три
пороги = 150 перевірок, і при 5% п'ять-сім «знахідок» з'являться з самого
шуму. Тому гнучкість шару має бути **порахована**: лічильник спроб обов'язковий
нарівні зі списком тих, що вижили.

- [ ] Лічильник спроб як обов'язкове поле кожного звіту верхнього шару
- [ ] Запечатати відкладений період **до** початку розвідки, не після

## 24. Порядок робіт після 29.08: правильність, ролі, розмітка, розширення

Погоджено з власником 2026-08-29. Його поправка до мого порядку прийнята й
стоїть першою: **спершу довести, що все рахує правильно, і лише потім
розширювати.** Причина не в обережності — на ширшому всесвіті ті самі дефекти
коштують більше й ховаються краще.

### Крок 1. Правильність — доробити почате

Чотири речі відомі, виміряні й записані; жодна не потребує розвідки, лише
виконання.

- [ ] `state_FRED_*` рахувати один раз на дату (#145) — виправлено й
      перевірено, чекає прогону, щоб потрапити в батч
- [ ] Переповнення при обчисленні дисперсії: `overflow encountered in
      multiply` (`sqr = np.multiply(arr, arr)` у `nanstd`). **Виключено
      29.08:** усі 2 287 стовпців батчу мають статистику parquet, жоден не
      перевищує 1e12 за модулем, нескінченностей немає; цілі не більші за
      10.1. У float64 такі величини переповнитись не можуть, тож джерело —
      всередині обчислення, а не в даних. Наступний крок: ловити
      попередження через `np.errstate(over='raise')` і подивитись, на якому
      саме стовпці воно спрацьовує, замість шукати за величиною
- [ ] Один писар батчу замість двох (#138)
- [ ] Дедуплікація FRED при вставці за (ряд, дата спостереження), інакше
      таблиця росте на ~85 тис. рядків щодня (#142)

### Крок 2. Ролі — виміряти, а не призначити

Таксономія записана в `project_feature_role_taxonomy`: зламане / випереджальне
/ умова / взаємодія / контекст / ніщо. Роль є **результатом виміру**, і одна
величина може мати кілька ролей.

Вимірів бракує трьох, і кожен уже показав, що він не декоративний:

- [ ] **Стабільність за періодами.** Пулований t ховає згасання. Виміряно
      29.08 на `insider_net_value_30d`: сукупне `t = -6.26` виглядало
      найсильнішим у батчі, а розбивка дала -2.74, -3.01, **-6.01 навесні
      2020**, -1.88 і **-0.08 за останні двадцять місяців**. Ефект існував і
      згас. Перевірка стає обов'язковою для кожного виживша
- [ ] **Дохідність на одиницю ризику** проти базової — відділити сигнал від
      премії за ризик. Шість із семи виживших є статистиками розподілу ціни,
      і всі знаки узгоджені з премією за низький ризик, а не з передбаченням
- [ ] **Беззбитковість проти витрат** на кожному горизонті. Модель IBKR уже
      в `targets.yaml`; там же записано, що для годинного горизонту знак
      результату **не визначається** OHLC-барами

### Крок 3. Розмітка — каталог ролей, який веде себе сам

Ідея власника про «напівпрозоре мічення» даних. Одна таблиця: **рядок на
величину, стовпці — виміряні ролі**, і заповнюють її **звіти**, а не рука.
Величина може мати кілька позначок одночасно; порожня клітинка означає «не
міряли», а не «немає».

Це і є те, що робить систему модульною по-справжньому: нова ознака або нове
джерело входить, проходить ті самі виміри й отримує ті самі позначки, без
окремого рішення про неї.

- [x] **Каталог як артефакт, що перебудовується зі звітів.** Зроблено
      29.08: `leading_feature_report.py` лишає `feature_roles_1d.csv`,
      `feature_catalogue.py` рендерить `docs/FEATURE_ROLES.md`. Руками не
      ведеться нічого — інакше через місяць таблиця розійдеться з даними
- [x] **Позначка «згасло» окремо від «немає».** Обидві існують і обидві
      спрацювали: 1 величина «згасла», 203 «ринково-широкі» (не можуть
      ранжувати за конструкцією), 155 «всередині шуму», 74 «знак не тримається»,
      14 «позначає ім'я»
- [x] **Нуль записаний як запис.** 450 величин виміряно, вижило **нуль**, і в
      каталозі це сказано прямим текстом із причиною для кожної

### Крок 4. Розширення всесвіту

**Чому саме після кроків 1–3, а не раніше:** ширший всесвіт покращує **всі**
виміри одразу, тож він має застосовуватись до приладу, який уже не бреше.

**І чому воно все одно найдешевший хід.** Нова ознака — це ще одна перевірка,
і поріг для решти стає суворішим. Нове ім'я — це більше спостережень на ту саму
гіпотезу. 110 імен дають 110 спостережень на дату; 500 дадуть 500, і той самий
крос-секційний IC вимірюється втричі надійніше **без жодної нової гіпотези**.
Ознаки множать перевірки, імена множать дані.

- [ ] Середні й малі компанії, не ще двадцять великих. Ефекти на кшталт
      інсайдерського там мали б не встигнути згаснути — менше грошей, що їх
      виїдають
- [ ] Метод відбору недооцінених (Грем) як окреме джерело всесвіту, не як
      ознака
- [ ] Випередження між іменами: чи рух великого імені передвіщає рух менших
      у секторі. Механізм peer-ознак уже існує, `peer_volatility` вижив —
      це нова ознака в наявному механізмі, не нова архітектура

### Що НЕ робиться

- Навчання, вибір ознак, гіперпараметри — похідне від кроку 2. Доки ролі не
  виміряні, вибирати нема з чого
- Друге використання запечатаного періоду. Печатка купує **одне**
  підтвердження (`docs/SEALED_HOLDOUT.md`)

---

## 25. Три правки 31.08: чому прогін падав, чому гейт нічого не ловив і чому перезапуск коштував усе

Прогін 6 помер на восьмій годині, на другій денній цілі:

```
numpy._core._exceptions._ArrayMemoryError: Unable to allocate 749. MiB
for an array with shape (490799, 200) and data type int64
```

Три речі, які довелося виправити разом, бо всі три сидять в одному місці —
між підготовкою даних і гейтом чемпіонів. Числа й деталі — в реєстрі
(#158, #172, #178); тут те, що з цього випливає для порядку робіт.

### Що це було насправді

**Не брак памʼяті, а порядок кроків.** `prepare_data_for_models` заповнював
пропуски й масштабував **усі 474 колонки**, після чого бюджет моделі
(`models.yaml`) лишав від пʼяти до тридцяти пʼяти. Тобто 439 колонок
проходили найдорожчий крок конвеєра, щоб бути викинутими наступним.
Медіанний імпутер `sklearn` іде через masked array і **сортує** блок, тобто
просить індекс `int64` розміром із сам блок — звідси 749 МіБ на двохсот
колонках.

Виправлення — **перестановка, не новий відбирач**. Колонки ранжуються тим
самим статистиком, який далі витрачає бюджет, і заповнюються тільки ті, що
пройшли стелю. Перші 35 із перших 70 за тим самим порядком — це ті самі
перші 35, тож жодна модель не вчиться на іншому. Це перевірено на трьох
бюджетах побітово, а не заявлено.

**Чому це записано в мапу, а не лише в реєстр:** доти вартість прогону
вважалась властивістю задачі («багато даних — довго»). Вона була
властивістю порядку кроків. Прогін, оцінений у ~25 годин, після
перестановки має коштувати години. Це міняє, скільки гіпотез можна
перевірити за тиждень, а не лише зручність.

### Гейт перевіряв дві сходинки з пʼяти

Драбина суперників (`docs/WORKING_METHOD.md`) має пʼять щаблів. У коді
перевірялись два. Наслідок був виміряний 30.08 і він однозначний: **сім
чемпіонів 15-хвилинного кадру пройшли гейт**, і пʼять із них програють
суперникові, якого гейт не рахував — найчастіше **годиннику**.

Тепер у гейті всі пʼять, і планка — **максимум по всіх**, а не перший
виміряний. Дорогою знайшлось третє: персистентність лагувала **по рядках**,
а обʼєднаний кадр перемішує 22 імені на одній позначці часу — тобто
«те саме h барів тому» було іншою компанією кілька хвилин тому.

**Наслідок для порядку робіт.** Усі чемпіони, вироблені до 31.08,
недоперевірені **за побудовою** — не через помилку, а через відсутні
сходинки. Їх не можна вважати ні підтвердженими, ні спростованими; їх треба
**переміряти** новим гейтом. Це стосується і сімки з 15-хвилинного кадру, і
всього, що лежить у `data/trained_models`.

### Перезапуск коштував увесь прогін

15-хвилинний кадр пораховано **шість разів** із побітово ідентичним
результатом, бо кожна аварія на денному кадрі відкидала прогін на початок.
Тепер стадія 4 пише журнал контекстів — що навчено, з яким результатом і на
**яких саме даних**.

Читання журналу **вимкнене за замовчуванням**, і це не обережність заради
обережності. Відтворений контекст — це число, якого прогін, що його
показує, не рахував. Кожна попередня версія «на диску є файл, отже зроблено»
в цьому проєкті закінчувалась тим, що застарілий артефакт читали як свіжий.
Тому: журнал пишеться завжди, підстановку вмикає людина
(`modeling.resume_completed_contexts`), і збіг відбитка має бути побайковим.

### Що це означає для наступного кроку

Порядок робіт §24 не змінюється. Змінюється те, що **крок 1 більше не
впирається в прогін, який не доїжджає**. До 31.08 будь-який вимір ролей
упирався в те, що повний прогін коштує добу і падає; тепер це не так.

Але додається робота, якої в §24 не було:

- [ ] **Переміряти все, що вже названо чемпіоном**, повним гейтом. Сім
      чемпіонів 15-хвилинного кадру — перші в черзі; за #173 пʼять із них
      мають відпасти, і якщо відпадуть не пʼять, розбиратись треба з гейтом,
      а не радіти
- [ ] `target_intraday_volatility_15m` істинна на кожному рядку (#174) — до
      виправлення визначення ціль не має брати участі ні в навчанні, ні в
      оцінці
- [ ] Прогнати повний цикл на виправленому конвеєрі й **порівняти з
      прогоном 6** там, де він устиг дійти: 15-хвилинний кадр має дати ті
      самі сім переможців арени і **менше** чемпіонів після гейта. Якщо
      чемпіонів стільки ж — гейт не працює

---

## 26. Режимна арена: чому ключ константний і що було б справжнім експериментом

Записано 31.08 після вимірювання, а не після міркування.

### Що вирішувалось

`current_pattern` — ключ, під яким записується чемпіон. Він мав нести
ринковий режим, а несе літеральне `'normal'` у ста відсотках випадків, бо
`MARKET_REGIME` вимкнули 28.08 за ціною (5.4 години з дванадцяти) і за
провалом власної перевірки. Питання: замінити на `volatility_regime_*`,
який у батчі є, увімкнути старий назад, чи прийняти константу.

### Поправка до першої оцінки

Спершу я написав, що заміна «поділить контекст натроє і втричі збільшить
кількість чемпіонів». **Це неправда.** `current_pattern` рахується один раз
на (тікер, кадр), і `df` ним ніде не фільтрується — це **назва**, а не
розділення. Вартість заміни нульова.

Тобто питання зводиться до одного: чи ця назва щось розрізняє.

### Вимір

Значення на останньому барі кожного тікера:

| кадр | розподіл | різних значень |
|---|---|---|
| 15m | `low` 110 зі 110 | **1** |
| 60m | low 98, normal 8, high 3, extreme 1 | 4, але 89% в одному |
| 1d | extreme 50, normal 26, high 20, low 14 | 4, рівномірно |

Денний розподіл виглядає придатним — доки не врахувати пулінг. При
`pool_tickers: true` контекст один на (кадр, ціль), тож читається останній
рядок **об'єднаного** кадру. А на останній позначці 108 тікерів тримають
чотири різні режими. Мітка визначалась би тим, який рядок опиниться
останнім: зміни порядок тікерів — зміниться режим чемпіона без жодної зміни
в даних чи моделі.

**Це не вісь. Це записаний підкид монети.**

### Рішення

Ключ лишається константним, свідомо. Обіцянка режимності прибрана з
коментарів замість вдавання, а падіння в типове значення більше не мовчазне.

### Що було б справжнім експериментом

Не мітка, а **розділення**: навчати окрему модель на кожен режим. Денний
кадр дав би ~176 тис. рядків на режим із 705 тис. — вистачає з запасом. Це і
є те, що назва «Regime-Aware Training Arena» обіцяє.

Чому не зараз:

- це множить гіпотези вчетверо, а в нас **нуль** кандидатів, що пережили
  повну драбину. Ділити на чотири те, чого немає, — це чотири способи знайти
  збіг;
- `volatility_regime` на 15m і 60m був майже константним через #183, тож
  розділення там і не відбулося б. Після виправлення нормалізації розподіл
  стає 37/29/21/14 — тобто розділення стає можливим уперше, але **лише
  після повної перебудови батчу**.

- [ ] Після того, як хоч один чемпіон переживе повну драбину: розділити його
      контекст за `volatility_regime` і перевірити, чи перевага тримається в
      кожному режимі, чи живе в одному. Це перевірка ролі «умова» з
      таксономії, а не нова архітектура

---

## 27. Стан гейта на кінець 31.08: що він тепер перевіряє і чого досі ні

§25 писався після трьох ранкових правок і встиг застаріти. Ось чинний стан.

### Драбина в гейті: п'ять щаблів, три з яких сьогодні не працювали

| щабель | стан до 31.08 | стан зараз |
|---|---|---|
| 1. базова частота | поглинається найкращою константою | ✔ |
| 2. найкраща константа | ✔ | ✔, і на новій метриці це рівно 0.5 |
| 3. лаг-h | лише для регресії, лаг по РЯДКАХ | ✔ для обох, лаг усередині серії |
| 4. годинник | **не існував у коді** | ✔, три розрізи, береться найкращий |
| 5. одна ознака | **мертвий у 100% випадків** (#184) | ✔, але обирає за кореляцією (#188) |

Плюс два, що не є щаблями драбини:

- **запас ≥ однієї стандартної похибки** різниці, блоковим бутстрапом (#186);
- **стабільність по фолдах** — не запускалась для жодного об'єднаного
  контексту (#189), тепер запускається, але дає лише два фолди (#191).

### Метрика

Класифікація судиться **збалансованою точністю**, і арена вибирає переможця
нею ж (#187). Доти арена брала F1, а гейт міряв переможця на метриці, якою
той ніколи не змагався.

### Що це дало на 15-хвилинному кадрі

Дев'ять цілей, **два чемпіони** проти семи в прогоні 6 на тих самих даних.

Найясніший випадок — `hourly_volume_spike_1h`: збалансована точність
**0.8024**, і рівно **0.8024** дає одна колонка `hour_cos` із прямою лінією.
Модель на 1 353 ознаках відтворила косинус години доби до четвертого знака.

### Що ще НЕ перевіряється, і це треба знати перед читанням будь-якого числа

- [ ] **Перевага над найсильнішим суперником у часі** (#192). Драбина міряє
      її на відкладеному наборі; стабільність міряє інше — чи модель б'є
      мажоритарну базу на фолдах. Для `volatility_spike_1h` із запасом у 2.3
      похибки це вирішальна прогалина, і саме її залишив відкритою #175
- [ ] **Найкращий однооколонковий суперник, а не найкорельованіший** (#188).
      На `breakout_1h` драбина взяла `CCI_15m` (0.7915), тоді як #171 уже
      виміряв відстань до смуги Боллінджера з AUC 0.9666 на тій самій цілі
- [ ] **Геометрія фолдів на об'єднаних кадрах** (#191): два фолди — це 25%
      за чистої випадковості, проти 6% для чотирьох
- [ ] **Економічний щабель для класифікації.** `excess_over_passive` свідомо
      незастосовний до міток класу, і це чесно; але тоді для бінарних цілей
      у гейті немає жодної перевірки на гроші

### Порядок

Спершу #192 — без нього вузькі запаси не означають нічого. Потім #188 і
#191. Тільки після цього має сенс питати, чи `breakout_1h` і
`volatility_spike_1h` є знахідками: сьогодні вони **кандидати з повним
записом**, а не результати.
