# Configuration for data processing steps

stage_2_processing:
  requires: ['raw_data']

safe_fill:
  fill_with_zero:
    - "news_score"
    - "impact_score"
    - "reverse_impact"
    - "daily_sentiment"
    - "match_count"
    - "news_count"

data_preparation:
  test_size: 0.2
  seq_len: 10
  feature_columns:
    - "feature_rsi"
    - "feature_ema_cross"
    - "feature_volatility"
    - "feature_sentiment_score"
    - "feature_news_volume"
  targets_config:
    - name: "target_regression_1d"
      type: "regression"
      params:
        base_col: "close"
        shift: -1
    - name: "target_binary_1d_0_0"
      type: "classification_binary"
      params:
        base_col: "close"
        shift: -1
        threshold: 0.0
    - name: "target_multiclass_1d"
      type: "classification_multiclass"
      params:
        base_col: "close"
        shift: -1
        thresholds: [-0.01, 0.01]
    - name: "target_rsi_prediction_3d"
      type: "indicator_prediction"
      params:
        indicator_col: "RSI_14"
        shift: -3

# Налаштування стадії моделювання.
#
# Ця секція раніше не існувала в жодному конфізі, хоча
# src/pipeline/stages/modeling/orchestrator.py читає get_config('modeling').
# Виклик повертав None, тож УСІ чотири налаштування нижче мовчки падали в
# код-константи — змінити їх через конфіг було неможливо.
# Значення тут дорівнюють тим самим константам, тож поведінка не змінюється;
# змінюється лише те, що тепер їх можна налаштувати.
modeling:
  strategy: hybrid          # src.training.constants: стратегія тренування
  batch_size: 10            # BATCH_TRAINER_DEFAULT_BATCH_SIZE
  max_memory_gb: 12.0       # BATCH_TRAINER_DEFAULT_MAX_MEMORY_GB

  # Одна модель на (кадр, ціль) через усі імена, замість однієї на кожне ім'я.
  #
  # Механізм існує в `iter_model_contexts(pool_tickers=...)` і виміряний у
  # його ж докстрінгу: об'єднана модель б'є 22 потікерні на КОЖНОМУ
  # співвідношенні витрат від 0.5 до 3.0, і розрив росте зі здорожчанням
  # хибних сигналів. Виклик у стадії 4 його не передавав, тож кращий за
  # виміром режим лежав вимкненим.
  #
  # Два числа з 29.08 додають до цього: потікерний контекст має ~900
  # навчальних рядків — саме через це бюджет ознак опустили до п'яти, — і
  # 47 контекстів на AAPL та MSFT дали НУЛЬ чемпіонів. Прогін зайняв
  # 31 хвилину на два імені, тобто близько тридцяти годин на 110: ця форма
  # не просто гірша, вона непроганяльна цілком.
  #
  # Кадр НЕ об'єднується: перевага на 15 хвилинах і на дні — різні явища, а
  # ознаки несуть суфікс кадру, тож об'єднаний по кадрах кадр був би
  # переважно порожнім.
  #
  # false, доки не порівняємо обидва режими на однакових даних.
  pool_tickers: true

  # Чи можна перезапустити прогін, не рахуючи наново те, що вже пораховано.
  #
  # Стадія 4 ЗАВЖДИ пише журнал контекстів
  # (data/trained_models/context_ledger.json): що навчено, з яким результатом
  # і на яких саме даних. Цей перемикач вирішує лише, чи його ЧИТАТИ.
  #
  # Навіщо: прогін 31.08 помер на восьмій годині на другій денній цілі, і
  # разом з ним пропало все, що вже було пораховано. 15-хвилинний кадр
  # перераховано шість разів у прогонах 1-6 — щоразу з тими самими числами.
  #
  # Чому false за замовчуванням: відтворений контекст — це число, якого цей
  # прогін не рахував. Відбиток даних це стереже (збіг має бути побайтовий,
  # інакше контекст тренується заново), але вмикати підстановку має людина
  # після падіння, а не система тихо сама.
  resume_completed_contexts: false
  # test_size навмисно НЕ дублюється тут: він живе під data_preparation вище,
  # і PipelinePolicyManager.split_policy() резолвить його звідти.

# Секція, яку читає src/pipeline/stages/processing/orchestrator.py через
# get_config('processing'). Раніше її не існувало, тож filtering_config
# дорівнював None і IntelligentDataFilter працював БЕЗ конфігурації на
# кожному прогоні — а саме він вирішує, які дані взагалі входять у конвеєр.
# Значення нижче дорівнюють захардкодженим дефолтам у src/processing/filters/,
# тож поведінка не змінюється; змінюється те, що їх тепер можна налаштувати.
processing:
  filtering:
    # price_filter.py
    # 30, не 2: оцінка якості рахує стандартне відхилення, збіг
    # каденції та частку дублікатів — з двох барів це не має сенсу,
    # але такий ряд проходив гейт і оцінювався як достовірний.
    min_candles_per_timeframe: 30
    min_data_quality_score: 0.6
    # Поріг застосовується до z-оцінки ДОХІДНОСТЕЙ бар-до-бару.
    # Раніше рахувалось по рівнях ціни, і збій +30% за бар не
    # виявлявся на жодному тікері.
    anomaly_std_dev_threshold: 3
    max_gap_duration_hours: 24
    min_cadence_match_ratio: 0.60
    max_extreme_return_ratio: 0.01
    # news_filter.py
    news_title_min_len: 10
    news_content_min_len: 50
    # social_filter.py
    reddit_score_threshold: 1
    reddit_text_min_len: 10
  normalization: {}

# Читається src/pipeline/stages/prediction/orchestrator.py.
prediction:
  # Наскільки знижується впевненість, коли прогноз суперечить чемпіону.
  champion_contradiction_penalty: 0.7

# Читається src/main/modes/backtest.py.
backtest:
  walk_forward_validation: false
  walk_forward:
    in_sample_months: 12
    out_sample_months: 3
