# Initial Diagnostic Summary

This v2 kit adds static engagement scans, domain-rule scans, registry consistency checks, runtime tracking hooks, and correctness contract tests.

Expected high-priority checks:
- target calculators ticker safety
- feature enrichers not emitting `target_*`
- synthetic not primary calibration score
- ModelFactory lazy imports
- registry/factory consistency
