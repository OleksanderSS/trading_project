services:
  pipeline:
    build: .
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - .env
    command: ["scripts/run_full_pipeline_automated.py", "--batch-name", "main_database"]
